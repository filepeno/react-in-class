import NavLink from "./NavLink.jsx";

export default function Header() {
  return (
    <header>
      <nav>
        <ul>
          <NavLink />
          <NavLink />
          <NavLink />
          <NavLink />
        </ul>
      </nav>
    </header>
  );
}
